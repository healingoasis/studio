packages:
  - "apps/*"
  - "packages/*"

# sharp only powers Next's image optimisation, which no prototype here uses. Recording
# the decision keeps pnpm from stopping on an undecided build script every install.
allowBuilds:
  sharp: false
