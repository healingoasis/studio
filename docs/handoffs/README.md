# Handoffs

Notes for Dan (Kite Agency), written by `/ask-dan`. One file per ask, named
`YYYY-MM-DD-short-slug.md`. Each note says: what Daniel wants, what is blocking,
and exactly what Dan needs to do. Dan marks them done by adding a `Done:` line.
