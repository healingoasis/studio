# Ideas

One file per idea, named `YYYY-MM-DD-short-slug.md`, based on `_template.md`.
This folder is the heart of the studio: capture first, filter later.
Statuses: `spark` → `shaping` → `prototyping` → `with-dan` → `live` (or `parked`).
