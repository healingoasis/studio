---
description: Write a handoff note for Dan (Kite Agency) and back it up
argument-hint: [what is needed]
---

Something needs Dan: $ARGUMENTS

1. Write `docs/handoffs/<today's date>-<short-slug>.md` with three short sections:
   **What Daniel wants** (plain language), **What is blocking** (missing access,
   needs deploy, live-store change, etc.), **What Dan needs to do** (be specific,
   reference idea files and prototype folders by path).
2. If it relates to an idea, set that idea's status to `with-dan` and add a Log line.
3. Commit and push with a message starting `Handoff:`.
4. Tell Daniel it is written up and backed up where Dan will see it, and suggest he
   also text or email Dan a heads-up since nobody watches GitHub all day.
