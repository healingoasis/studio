---
description: Recap every idea and where it stands
---

Give Daniel a plain-language recap of the studio:

1. Read every file in `ideas/` (name + status + last Log line) and any open notes in
   `docs/handoffs/` (ones without a `Done:` line). Skim recent git log for context.
2. Summarize conversationally, grouped by momentum: what is being prototyped, what is
   waiting on Dan, fresh sparks, and anything parked. A few sentences, not a report.
   No file paths, no jargon.
3. End with one suggestion for the most promising next step, and ask what he feels
   like working on.
